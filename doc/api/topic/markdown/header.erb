<% if @resource %>
# <%= @resource %> API
<% else %>
# All API Resources
<% end %>

<% if @beta %>
<%= hint("warning", "BETA: This API resource is not finalized, and there could be breaking changes before its final release.") %>
<% end %>
<% if @internal && @show_internal %>
<%= hint("warning", "INTERNAL: This API resource is marked as 'internal use only' and is not included in the production version of these docs.") %>
<% end %>
<%= yieldall %>
