<tr>
  <!--Select the term to run the report on-->
  <td>
    <label>
      <%= t('account_reports.default.parameters.header', %{Term})%>:&nbsp;
      <%= render(
        partial: 'shared/enrollment_term_select',
        locals: {
          name: 'parameters[enrollment_term_id]',
          terms: @root_account.enrollment_terms.active,
          selected_id: @term && @term.id,
          exclude_all_option: !all_terms_option
        }
      ) %>

    </label>
  </td>
</tr>
