define(function(require) {
  var Subject = require('jsx!components/score_percentile_chart');

  describe('ScorePercentileChart', function() {
    this.reactSuite({
      type: Subject
    });

    it('should render', function() {});
  });
});