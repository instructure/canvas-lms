// the file needs to be a plugin to load lang-all into the fullcalendar object,
// but we don't want to have to call it as a plugin everywhere, hence this
// wrapper
define(['fullcalendar_plugin!'], function() {});
