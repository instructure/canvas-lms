<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
</head>
  <body>
    <h1 id="<%="#{title}_TOC"%>"><%="#{title}"%></h1>
    <ol>
    <% content.each do |item| %>
      <li><a href="#<%= "#{item[:identifier]}" %>"><%= item[:title] %></a></li>
    <% end %>
    </ol>
    <div style="page-break-before:always;"></div>
    <% content.each do |item| %>
      <%=build(item)%>
      <hr/>
      <a href="<%= "##{reference}.xhtml" %>"><%="Back to #{title}"%></a>
      <div style="page-break-before:always;"></div>
    <% end %>
  </body>
</html>
