{
  "name": "@instructure/translations",
  "version": "1.0.0",
  "engines": {
    "node": ">= 10.10"
  },
  "description": "Contains translations to be used by other packages",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "split": "node bin/cli.js",
    "test": "jest --env jsdom-fourteen --color",
    "lint": "prettier bin/**/*.js lib/**/*.json --write"
  },
  "devDependencies": {
    "jest": "^24",
    "jest-environment-jsdom-fourteen": "^0.1.0",
    "prettier": "^2"
  }
}
