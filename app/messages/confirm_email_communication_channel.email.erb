<% define_content :link do %>
  http://<%= HostUrl.context_host(((asset.pseudonym || asset.user.pseudonym).account rescue nil)) %>/communication_channels/<%= asset.id %>/confirm/<%= asset.confirmation_code %>
<% end %>

<% define_content :subject do %>
  Confirm Email: Canvas
<% end %>

The email address, <%= asset.path %> is being registered at <%=  (HostUrl.context_host((asset.pseudonym || asset.user.pseudonym).account) rescue nil) || HostUrl.default_host %> for the user, <%= asset.user.name %>.

To confirm this registration, please visit the following url: 
<%= content :link %>
