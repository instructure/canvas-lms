<% define_content :link do %>
  <%= HostUrl.protocol %>://<%= HostUrl.context_host(asset.context) %>/<%= asset.context.class.to_s.downcase.pluralize %>/<%= asset.context.id %>/assignments/<%= asset.asset.assignment_id %>/<%= asset.asset.class.to_s.downcase.pluralize %>/<%= asset.asset.user_id %>
<% end %>

<%= t('#messages.peer_review_invitation.facebook.body', "You've been invited to peer review %{reviewee}. Follow the link below to review them!", :reviewee => asset.asset.user.name)%>