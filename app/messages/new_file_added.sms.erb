<% define_content :link do %>
<%= HostUrl.protocol %>://<%= HostUrl.context_host(asset.context) %>/<%= asset.context.class.to_s.downcase.pluralize %>/<%= asset.context_id %>/files/<%= asset.id %>
<% end %>
<%= t('body', 'New file added for %{name}:', :name => asset.context.name) %>
<%= asset.title %> (<%= asset.readable_size %>)

<% t('link', 'More info at %{link}', :link => content(:link)) %>