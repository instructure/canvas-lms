<%= asset.user.short_name rescue 'Unknown User' %> just sent you a message <%= asset.context.name %>:

<%= strip_and_truncate(asset.body, :max_length => 50) %>

More info at <%= HostUrl.context_host(asset.context) %>