<% define_content :link do %>
  http://<%= HostUrl.context_host(asset.assignment.context) %>/<%= asset.assignment.context.class.to_s.downcase.pluralize %>/<%= asset.assignment.context_id %>/assignments/<%= asset.assignment_id %>/submissions/<%= asset.user_id %>
<% end %>

<% define_content :subject do %>
  Grade Changed: <%= asset.assignment.title %>, <%= asset.assignment.context.name %>
<% end %>

re-graded: <%= force_zone(asset.graded_at).strftime("%b %d at %I:%M") rescue "No Date Set" %><%= force_zone(asset.graded_at).strftime("%p").downcase rescue "" %>
<% if asset.score  %>score: <%= asset.score %> out of <%= asset.assignment.points_possible || "N/A" %><% end %>
<%= "score pending review by the teacher" if asset.pending_review? %>