(BZ) <%= asset.title.length > 30 ? "#{asset.title[0..30]}..." : asset.title %>: <%= HtmlTextHelper.strip_and_truncate(asset.message, :max_length => 100) %> <%= BitlyHelper.new.bitly_shorten("#{HostUrl.protocol}://#{HostUrl.context_host(asset.context)}/courses/#{asset.context.id}/discussion_topics/#{asset.id}") %>
