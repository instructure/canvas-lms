(BZ) <%= HtmlTextHelper.strip_and_truncate(asset.message, :max_length => 130) %><%= asset.message.length > 130 ? "... " : " " %><%= BitlyHelper.new.bitly_shorten("#{HostUrl.protocol}://#{HostUrl.context_host(asset.context)}/courses/#{asset.context.id}/discussion_topics/#{asset.id}") %>
