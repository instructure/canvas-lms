<% define_content :subject do %>
<%= '(BZ)' %>
<% end %><% message = html_to_text(asset.message) %><%= (message.length <= 155 ? "#{message}" : (CanvasTextHelper.truncate_text(message, :max_length => 130) + "... ")) + ' ' + BitlyHelper.new.bitly_shorten("#{HostUrl.protocol}://#{HostUrl.context_host(asset.context)}/courses/#{asset.context.id}/discussion_topics/#{asset.id}") %>
