<% define_content :link do %>
  http://<%= HostUrl.context_host(asset.group.context) %>/<%= asset.group.context.class.to_s.downcase.pluralize %>/<%= asset.group.context_id %>/groups/<%= asset.group.id %>
<% end %>
<% define_content :subject do %>
  New Student Group for <%= asset.group.context.name %>
<% end %>

New Student Group: <%= asset.group.name %>

A new student organized group named "<%= asset.group.name %>" was created for the <%= asset.group.context.class.to_s.downcase %> <%= asset.group.context.name %>. The first member is <%= asset.user.name %>.

You can see details for this group by clicking the link below:
<%= content :link %>
