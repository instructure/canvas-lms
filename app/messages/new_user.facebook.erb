<% define_content :link do %>http://<%= HostUrl.default_host %>/users/<%= asset.id %><% end %>

New User for <%= asset.account.name %>
  
A new user <b><%= asset.name %></b> has registered for the account <b><%= asset.account.name %></b>.
