<% define_content :link do %>
  http://<%= HostUrl.context_host(asset.rubric_association.context) %>/<%= asset.rubric_association.context.class.to_s.downcase.pluralize %>/<%= asset.rubric_association.context_id %>/rubric_associations/<%= asset.rubric_association_id %>/assessments/<%= asset.uuid %>
<% end %>
Canvas Alert - Invite: assess <%= asset.rubric_association.title %>, <%= asset.user.name %>
