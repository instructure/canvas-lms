<% define_content :link do %>
  http://<%= HostUrl.context_host(asset.group.context) %>/<%= asset.group.context.class.to_s.downcase.pluralize %>/<%= asset.group.context_id %>/groups/<%= asset.group_id %>
<% end %>
<% define_content :subject do %>
  New Group Membership for <%= asset.group.context.name %>
<% end %>

New Group Membership: <%= asset.group.name %>

You've been invited to join the group <%= asset.group.name %> for the <%= asset.group.context.class.to_s.downcase %> <%= asset.group.context.name %>.

You can check out the group by clicking the link below:
<%= content :link %>
