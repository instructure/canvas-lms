<% define_content :link do %>
  http://<%= HostUrl.context_host(asset.context) %>/<%= asset.context.class.to_s.downcase.pluralize %>/<%= asset.context_id %>/conferences
<% end %>
Canvas Alert - Invite: attend <%= asset.title %>, <%= asset.context.name %>
