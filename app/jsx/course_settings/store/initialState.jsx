define([], () => {

  const initialState = {
    courseImage: 'abc',
    imageUrl: '',
    showModal: false,
    gettingImage: false,
    uploadingImage: false,
    removingImage: false
  };

  return initialState;
});
