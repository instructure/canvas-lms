define([
  './rule',
  './assignment',
  './selected-path',
  './student',
], (ruleShape, assignmentShape, selectedPathShape, studentShape) => ({
  ruleShape,
  assignmentShape,
  selectedPathShape,
  studentShape,
}))
