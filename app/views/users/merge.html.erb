<% content_for :page_title do %>Merge User Accounts<% end %>

<% content_for :page_header do %>
  <h1>Merge User Accounts</h1>
<% end %>

<% content_for :page_subhead do %>
  <h2><%= @current_user.name %></h2>
<% end %>

<div style="width: 500px; margin-left: auto; margin-right: auto; margin-top: 10px;">
  <h2>Merge User Accounts</h2>
  <p>For one reason or another you may end up with two logins instead of just
  one.  If that happens, you can merge the two logins together and consolidate
  the information from them.</p>
  <p>
  To merge the account, <%= @current_user.name %> (<%= @current_user.email %>) into another
  account, please log in to the new account:
  <%= render 'shared/login' %>
  </p>
</div>