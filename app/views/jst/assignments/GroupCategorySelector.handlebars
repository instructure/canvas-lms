<div class="controls">
  <label class="checkbox">
    <input id="assignment_has_group_category"
    name="has_group_category"
      type="checkbox"
      {{checkedIf groupCategoryId}}
      {{disabledIfIncludes frozenAttributes "group_category_id"}}/>
    {{#t "is_group_assignment"}}This is a Group Assignment{{/t}}
  </label>
</div>

<div id="group_category_options" style="{{hiddenUnless groupCategoryId}}">

  <div class="controls">
    <label class="checkbox">
      <input name="grade_group_students_individually"
        type="checkbox"
        {{checkedIf gradeGroupStudentsIndividually}}
        {{disabledIfIncludes frozenAttributes "group_category_id"}}/>
      {{#t "grade_group_students_individually"}}
        Assign Grades to Each Student Individually
      {{/t}}
    </label>
  </div>

  <div class="controls">
    <label for="group_category_id" class="nobr">
      {{#t "group_set"}}Group Set{{/t}}
      <select id="assignment_group_category_id"
        name="group_category_id"
        {{disabledIfIncludes frozenAttributes "group_category_id"}}>
        {{#each groupCategories}}
          <option value="{{id}}"
          {{selectedIf ../groupCategoryId this.id}}>
            {{name}}
          </option>
        {{/each}}
        <option value="new">
          [ {{#t "group_category_options"}}New Group Category{{/t}} ]
        </option>
      </select>
    </label>
  </div>

</div>
