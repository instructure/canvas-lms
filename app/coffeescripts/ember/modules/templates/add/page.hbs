{{#ic-modal-title}}{{title}}{{/ic-modal-title}}

<ul>
  {{#each page in pages}}
    <li>
      <label>
        <input type="checkbox" {{action "toggleSelected" page on="change"}}/>
        {{page.title}}
      </label>
    </li>
  {{/each}}
</ul>

<div class="form-controls">
  <button type="submit" class="btn btn-primary">{{#t "add_pages"}}Add pages{{/t}}</button>
  <ul>
    {{#each model.selected}}
      <li>{{title}}</li>
    {{/each}}
  </ul>
</div>



